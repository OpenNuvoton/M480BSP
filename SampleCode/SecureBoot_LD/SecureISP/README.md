# SecureISP Sample

This sample code is designed to be tested with `SecureISPTool.exe`.

Use the Secure ISP tool located at:

```text
BSP\Utilities\SecureISPTool\SecureISPTool.exe
```

Run `SecureISPTool.exe` and use it together with this sample project to perform the Secure ISP test flow.
